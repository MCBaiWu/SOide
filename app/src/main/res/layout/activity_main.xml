<?xml version="1.0" encoding="utf-8"?>
<!--
  简化版主界面。
  - 根用 LinearLayout(vertical)，避免 CoordinatorLayout / fitsSystemWindows 在
    不同 Android 版本上把 fragment 顶到状态栏背后或被系统手势条盖住。
  - 中间 fragment_container 用 weight=1 占满剩余高度。
  - 底部 BottomNavigationView 固定在屏幕底部。
  - 根节点给一个明显的浅蓝背景，调试时一眼就能看到布局有没有真的铺满。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFEFEFEF">

    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#FFFFFFFF" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#FFFFFFFF"
        app:itemIconTint="@color/bottom_nav_color"
        app:itemTextColor="@color/bottom_nav_color"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_nav" />

</LinearLayout>
